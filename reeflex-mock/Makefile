# reeflex-mock/Makefile
# Convenience wrapper -- canonical entry is: python reeflex-mock/demo.py
# (make is not installed on the build machine; this file is for Unix users)
#
# Usage (from repo root):
#   export REEFLEX_OPA_BIN=/path/to/opa
#   export REEFLEX_POLICY_DIR=/path/to/reeflex-core/policy
#   make -f reeflex-mock/Makefile demo

.PHONY: demo

demo:
	python reeflex-mock/demo.py

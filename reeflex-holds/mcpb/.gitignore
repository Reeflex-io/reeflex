# Build outputs -- not source. Regenerated by build.ps1 / build.sh.
# Committed source is only: manifest.json, server/main.py, build.ps1,
# build.sh, .mcpbignore, .gitignore, README.md.
server/lib/
*.mcpb

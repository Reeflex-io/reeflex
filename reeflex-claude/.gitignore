# runtime audit output (regenerated by demo / hook runs) + inspection byproducts
*-audit.jsonl
*.jsonl
__pycache__/
*.pyc

# python packaging build byproducts (python -m build / pip install -e .)
build/
dist/
*.egg-info/
